# python_cli_minimal

Minimal docs-only example for applying the python_cli profile.

## Scope

This example validates that the template can produce project control documents for a Python CLI shape without implementing application behavior.

## P3 Boundary

No Python source code, package metadata, CLI command, external service call, or private input is included.
